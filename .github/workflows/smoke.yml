name: Smoke Tests

on:
  pull_request:
    branches: ['**']
  push:
    branches: [main]

jobs:
  smoke:
    name: Playwright smoke tests against local Supabase
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --force

      - name: Install Playwright browser (chromium only)
        run: npx playwright install --with-deps chromium

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: 2.75.0

      - name: Start local Supabase
        run: supabase start

      # These skip themselves when no stack is reachable, so `npm test` in
      # ci.yml stays green without Docker. Here they get a real database.
      # Run before the smoke step, whose db reset restores the seed afterwards.
      - name: Run integration tests against local Supabase
        run: npx vitest run netlify/functions/__tests__/admin-update-member.integration.test.js
        env:
          CI: true

      - name: Run smoke tests
        run: npm run test:smoke
        env:
          CI: true

      - name: Upload Playwright report on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7

      - name: Stop Supabase
        if: always()
        run: supabase stop
